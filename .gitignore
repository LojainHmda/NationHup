node_modules
dist
.local
.env
.env.*
!.env.example

# Google Cloud / credential material (never commit)
*.pem
*-credentials.json
*-service-account*.json
application_default_credentials.json
gcloud-application-credentials.json
**/google-credentials*.json

# Local dev logs (may contain tokens or PII)
server_output.txt
startup.log
debug.log
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Runtime upload scratch (can exceed GitHub 100MB limit)
uploads/temp/

# Pasted credential dumps (GitHub push protection / secret scanning)
attached_assets/*private-key-id*.txt
attached_assets/*GOOGLE-CREDS*.txt
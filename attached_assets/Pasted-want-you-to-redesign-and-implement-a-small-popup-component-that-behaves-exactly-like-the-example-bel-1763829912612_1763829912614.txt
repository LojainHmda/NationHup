want you to redesign and implement a small popup component that behaves exactly like the example below. This popup is a compact numeric input controller with increment/decrement buttons, copy/paste actions, a close button, and an Apply button. --- REQUIRED BEHAVIOR --- 1. The numeric value inside the input MUST NOT be passed or applied to the cells unless: - The user presses Enter - The user clicks the Apply button 2. Closing the popup (pressing the ✕ button) OR clicking anywhere outside the popup: - MUST NOT apply or pass the value to the cells - MUST reset/clear the current selection 3. The input value can be changed by: - Typing manually - Clicking the + button (increment by 1) - Clicking the – button (decrement by 1) The value must NEVER become negative. 4. Pressing Enter has the exact same behavior as clicking Apply: - Value is passed to selected cells through a callback such as onApply(value) - The popup closes - The selection is cleared 5. Copy button: - Copies the current input value to clipboard - Temporarily shows "Copied!" for 1 second 6. Paste button: خليك عالطريقة الي اصلا موجودة مسبقا --- LAYOUT AND DIMENSIONS --- • Popup container: - Width: 160px - Padding: 4px - Border: 1px solid #ddd - Border-radius: 6px - Background: #fff • Row 1: Contains: [ - ] [ input ] [ + ] [ ✕ ] - Height of buttons: 20px - Input width: 35px - Font size: 10px - Gap: 2px • Row 2: Contains: [ Copy ] [ Paste ] - Same dimensions as the small buttons • Row 3: Full-width Apply button - Height approx. 24px - Background: #4f46e5 - Text color: #fff - Border-radius: 4px Use the following code sample as the exact visual and structural reference: import { useState } from "react"; export function App() { const [show, setShow] = useState(false); const [value, setValue] = useState("10"); const [copied, setCopied] = useState(false); const handleCopy = () => { navigator.clipboard.writeText(value); setCopied(true); setTimeout(() => setCopied(false), 1000); }; const handlePaste = async () => { const text = await navigator.clipboard.readText(); setValue(text); }; const handleApply = () => { // Put your business logic here console.log("Applied:", value); setShow(false); }; return ( <div style={{ padding: 20, fontFamily: "sans-serif" }}> <button style={{ padding: "6px 10px", background: "#4f46e5", color: "#fff", border: "none", borderRadius: 4, }} onClick={() => setShow(true)} > Show Mini Input </button> {show && ( <div style={{ marginTop: 10, padding: 4, border: "1px solid #ddd", borderRadius: 6, display: "inline-block", position: "relative", width: 160, background: "#fff", }} > {/* Row 1 */} <div style={{ display: "flex", alignItems: "center", gap: 2, marginBottom: 4, width: "100%", }} > <button style={smallBtn} onClick={() => setValue(String(Number(value) - 1))} > - </button> <input value={value} onChange={(e) => setValue(e.target.value)} style={inputSmall} /> <button style={smallBtn} onClick={() => setValue(String(Number(value) + 1))} > + </button> <button style={closeBtn} onClick={() => setShow(false)}> ✕ </button> </div> {/* Row 2 */} <div style={{ display: "flex", gap: 2, marginBottom: 4 }}> <button style={smallBtn} onClick={handleCopy}> {copied ? "Copied!" : "Copy"} </button> <button style={smallBtn} onClick={handlePaste}>Paste</button> </div> {/* Row 3: Apply */} <button style={applyBtn} onClick={handleApply}> Apply </button> </div> )} </div> ); } // --- Styles --- const smallBtn = { flex: 1, height: 20, fontSize: 10, border: "1px solid #ccc", borderRadius: 4, cursor: "pointer", background: "#f8f8f8", }; const inputSmall = { width: 35, height: 18, fontSize: 10, textAlign: "center", border: "1px solid #ccc", borderRadius: 4, }; const closeBtn = { width: 20, height: 20, fontSize: 10, border: "1px solid #fca5a5", borderRadius: 4, background: "#fee2e2", cursor: "pointer", }; const applyBtn = { width: "100%", padding: "4px 0", fontSize: 11, border: "1px solid #4f46e5", borderRadius: 4, cursor: "pointer", background: "#4f46e5", color: "#fff", }; Recreate the popup so that it matches this layout AND the required behavior exactly.